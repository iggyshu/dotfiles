- defaults: 
    link:
      relink: true

- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.config/alacritty: alacritty.yml
    ~/.config/kitty: kitty
    ~/.config/nvim: nvim

- shell:
  - [git submodule update --init --recursive, Installing submodules]
