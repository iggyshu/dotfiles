- defaults: 
    link:
      relink: true

- clean: ['~']

- link:
    ~/.dotfiles: ''
    #    ~/.config/alacritty: alacritty
    ~/.config/kitty: kitty
    ~/.config/nvim: nvim
    ~/.tmux: tmux
    ~/.tmux.conf: tmux.conf

- shell:
  - [git submodule update --init --recursive, Installing submodules]
